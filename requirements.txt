# CogniStream — Python dependencies
# Pinned for reproducible offline builds

# Web framework
fastapi==0.115.9
uvicorn[standard]==0.34.3
python-multipart==0.0.20
python-dotenv==1.0.1

# Video / audio processing
opencv-python-headless==4.11.0.86
numpy>=1.26

# Speech-to-text
faster-whisper==1.1.1

# Embeddings
sentence-transformers==4.1.0

# Vector database (full package — needed for both embedded PersistentClient
# in local dev and HttpClient in Docker mode)
chromadb==1.0.11
# Chroma 1.0.x telemetry client expects pre-6 posthog capture signature.
posthog<6

# HTTP client (for Ollama)
httpx==0.28.1

# NLP (keyword / entity extraction)
spacy==3.8.7

# Graph
networkx==3.4.2

# ── Optional: NVIDIA NIM cloud support ──
# Uncomment if using NVIDIA cloud models (requires API key)
# nvidia-riva-client>=2.17.0  # Parakeet ASR (gRPC)

# ── Optional: CV pre-filter (~30% faster pipeline) ──
# YOLOv8n local object detection — drops keyframes with no interesting
# objects before they reach the slow VLM. Auto-downloads ~6 MB weights
# on first use. Note: pulls in numpy 2.x which conflicts with the pin
# above; install in a separate venv if you need both.
# ultralytics>=8.4.0
